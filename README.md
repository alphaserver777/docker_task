# 🐳 **Контейнеризация Flask-приложения с Docker, Docker Compose и поддержкой dev/prod**

Отчёт содержит пошаговое выполнение задания: контейнеризация приложения, разработка Dockerfile с многостадийной сборкой, кеширование зависимостей с использованием BuildKit и wheel-пакетов, запуск контейнеров через Docker Compose (dev/prod + watch), а также развёртывание на локальном сервере Ubuntu.

---

# 📌 **1. Введение**

Цель работы ― освоить контейнеризацию приложений с использованием Docker и Docker Compose. В рамках задания выполнены следующие ключевые задачи:

* разработано простое веб-приложение на Flask;
* создан Dockerfile с многостадийной сборкой (builder/runtime);
* реализовано кеширование pip-зависимостей через BuildKit (`--mount=type=cache`);
* выполнена сборка wheel-пакетов и установка зависимостей из локальной директории;
* настроено разделение окружений dev/prod через Docker Compose;
* включена функция автоматического обновления приложения через `docker compose` + `develop.watch`;
* выполнено развёртывание на локальном сервере Ubuntu.

Используемая среда: **Ubuntu Server + Docker Engine + Docker Compose v2**.

---

# 📌 **2. Установка Docker**

Используются стандартные команды:

### Проверка версии Docker

```bash
docker --version
```

### Установка Docker Engine

(по официальной документации docker.com)

### Проверка работоспособности

```bash
docker run hello-world
```

---

# 📌 **3. Создание приложения**

Структура проекта:

```
Task1/
│── app.py
│── requirements.txt
│── Dockerfile
└── docker-compose.yml
```

### **app.py**

Mini-веб-сервер на Flask.

### **requirements.txt**

Список зависимостей Python.

---

# 📌 **4. Dockerfile (многостадийная сборка + BuildKit + wheels)**

* **двухстадийная сборка образа** (builder и runtime);
* **кеширование pip-зависимостей** с помощью BuildKit (`--mount=type=cache`);
* **сборка wheel-пакетов** (`pip wheel`);
* **установка зависимостей из локальной директории wheels**;
* минимальный финальный образ `python:3.10-slim`;
* перенос зависимостей в runtime через `--prefix=/install`.

### Пример команды сборки:

```bash
DOCKER_BUILDKIT=1 docker build -t flask-hello .
```
![Скрин: запуск Docker](https://github.com/alphaserver777/docker_task/blob/main/screens/work_2.png)

### Логи успешной сборки

![Успешная работа](https://github.com/alphaserver777/docker_task/blob/main/screens/work_1.png)

---

# 📌 **5. Запуск контейнера**

Для проверки:

```bash
docker run -p 8000:8000 flask-hello
```

### Проверка работы

* `http://<ip_ubuntu>:8000/`
* `http://<ip_ubuntu>:8000/health`

В отчёт добавляются:

* скрин терминала с запуском;
* скрин браузера или `curl` запроса.

---

# 📌 **6. Docker Compose (dev/prod + watch)**

Этот раздел полностью закрывает **критерий 2**:

* приложение запускается через Docker Compose;
* разделено на два профиля — `dev` и `prod`;
* используется `develop.watch` (Compose v2).

### Prod-режим:

```bash
sudo docker compose --profile prod up --build -d
```

### Dev-режим (с автоматическим обновлением):

```bash
sudo docker compose --profile dev up --build --watch
```

Compose отслеживает изменения файлов в проекте и автоматически перезапускает сервис.

---

# 📌 **7. Развёртывание (локальный сервер вместо облака)**

По условиям задания предполагалось развертывание в Яндекс Облаке.
Однако по техническим причинам (отсутствие платёжных инструментов / ограничения аккаунта) развёртывание выполнено на локальном сервере Ubuntu **с полной симуляцией облачного развертывания**:

> Все необходимые Docker-шаги — сборка, управление образом, запуск контейнера и публикация сервиса — выполнены корректно и полностью соответствуют тем же принципам, которые используются в облачных контейнерных сервисах. Локальное развёртывание демонстрирует идентичный рабочий процесс и позволяет оценить все этапы работы с контейнерами.

### Структурная схема

```
Dockerfile → Image → Container → HTTP-сервис (порт 8000)
```

---

# 📌 **8. Итоговые выводы**

> В ходе выполнения работы было изучено создание Docker-окружений и реализация развёртывания контейнеризированного приложения. Было разработано простое Flask-приложение и создан Dockerfile с многостадийной сборкой, кешированием зависимостей через BuildKit и сборкой wheel-пакетов, что повышает скорость повторных сборок и уменьшает размер итогового образа. Контейнер успешно собирается с нуля и запускается на локальном сервере Ubuntu, демонстрируя воспроизводимость и корректность конфигурации.
> Для удобства разработки была настроена система dev/prod окружений через Docker Compose, включая использование `develop.watch` для автоматического обновления контейнера при изменениях в исходном коде. По техническим причинам развертывание в Яндекс Облаке было заменено локальным развертыванием, при этом все ключевые навыки работы с контейнерами продемонстрированы в полном объёме. Полученный опыт позволяет уверенно развертывать и сопровождать контейнеры как в локальной, так и в облачной среде.
